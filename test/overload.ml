let rec addi x y = x + y in 
let rec addf x y = x + y in 
let rec subi x y = x - y in 
let rec subf x y = x - y in 
print_int (addi 1 1);
print_float (addf 1.0 1.0);
print_int (subi 1 1);
print_float (subf 1.0 1.0)
